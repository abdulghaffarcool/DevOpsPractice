# name: CD

# on:
#   workflow_run:
#     workflows: ["CI Pipeline"]
#     types:
#       - completed

# jobs:
#   deploy:
#     if: ${{ github.event.workflow_run.conclusion == 'success' }}
#     runs-on: self-hosted

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Restart Kubernetes Deployment
#         run: kubectl rollout restart deployment/devopspractice-web -n devopspractice

#       - name: Wait for Rollout
#         run: kubectl rollout status deployment/devopspractice-web -n devopspractice

# ===================================
# Updated CD - Replaced by Argo CD
# ===================================
# name: CD

# on:
#   workflow_run:
#     workflows: ["CI Pipeline"]
#     types:
#       - completed

# jobs:
#   deploy:
#     if: ${{ github.event.workflow_run.conclusion == 'success' }}
#     runs-on: self-hosted

#     steps:
#       - name: Deploy Exact Docker Image
#         shell: powershell
#         run: |
#           kubectl set image deployment/devopspractice-web `
#             devopspractice-web=abdulghaffar2455/devopspractice:${{ github.event.workflow_run.head_sha }} `
#             -n devopspractice

#       - name: Wait for Rollout
#         shell: powershell
#         run: |
#           kubectl rollout status deployment/devopspractice-web -n devopspractice